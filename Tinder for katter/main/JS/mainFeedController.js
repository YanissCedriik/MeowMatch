function match(){
    // Push user to a copy of .users 
    // Use paramateres to fetch specified arrays and ID's.
    // to chatInboxview  
} 

function randomPotentialMatches(){
    //
}

function goToMainFeed(){
    model.app.currentPage = model.app.pages[3];
    changeView();
}
